# include<iostream> 
# include<string> 
using namespace std; 
int main() 
{ 
	int a;
	cout<<" Enter integer value: "; 
	cin>>a; 
	if (a%2==0) 
		cout<<"it is even no: "; 
	else 
		cout<<"it is odd no: "; 
		return 0; 
}
