# include<iostream> 
# include<string> 
using namespace std; 
int main() 
{ 
	float a; 
	cout<<" Enter a number: "; 
	cin>>a; if (a*1==a) 
	cout<<a<<" the number is Positive. "; 
	if (a*-1==a) 
	cout<<a<<" the number is Negative."; 
	if (a/2==0) 
	cout<<a<<" the number is Zero."; 
	return 0; 
}
